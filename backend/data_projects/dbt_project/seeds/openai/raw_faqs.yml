version: 2

seeds:
  - name: raw_faqs
    config:
      column_types:
        id: integer
        question: varchar(255)
        answer: text
        created_at: timestamp
        updated_at: timestamp

