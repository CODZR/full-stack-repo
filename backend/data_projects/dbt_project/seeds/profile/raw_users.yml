version: 2 

seeds: 
  - name: raw_users
    config:
      column_types: 
        id: integer
        email: varchar(255)
        username: varchar(255)
        password: varchar(255)
        created_at: timestamptz
        updated_at: timestamptz

